const { assert } = require('chai');

context('Github helpers', () => {
    describe('getReleaseNotes', () => {

        it('should work', () => {
            assert.isTrue(true);
        })
    });
});